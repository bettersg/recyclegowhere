/* TODO: move to a common folder once this is confirmed and used elsewhere */
export type TItems = {
	id: number;
	name: string;
	method: string;
};

export type AddressOption = {
	value: string;
	label: string;
};
